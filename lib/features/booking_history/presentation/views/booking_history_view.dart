import 'package:flutter/material.dart';

class BookingHistoryView extends StatelessWidget {
  const BookingHistoryView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("BookingHistory");
  }
}
