import 'package:flutter/material.dart';

class ControlWebsiteView extends StatelessWidget {
  const ControlWebsiteView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("ControlWebsite");
  }
}
