import 'package:flutter/material.dart';

class AddNewProgrammeView extends StatelessWidget {
  const AddNewProgrammeView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Add New Programme"),
    );
  }
}
